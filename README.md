This folder is a **catkin_workspace**.


To update submodules:

```
git submodule init
git submodule update
```


run `catkin_make` in this directory then use `source gaga/devel/setup.bash`
to execute the code.  


